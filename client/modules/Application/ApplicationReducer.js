// Import Actions
import {  } from './ApplicationActions';

// Initial State
const initialState = {};

const ApplicationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ApplicationReducer;
