// Import Actions
import {  } from './FamiliaActions';

// Initial State
const initialState = {};

const FamiliaReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FamiliaReducer;
