// Import Actions
import {  } from './ObservacionActions';

// Initial State
const initialState = {};

const ObservacionReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ObservacionReducer;
