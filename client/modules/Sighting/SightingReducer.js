// Import Actions
import {  } from './SightingActions';

// Initial State
const initialState = {};

const SightingReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SightingReducer;
