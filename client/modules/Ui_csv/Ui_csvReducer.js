// Import Actions
import {  } from './Ui_csvActions';

// Initial State
const initialState = {};

const Ui_csvReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default Ui_csvReducer;
